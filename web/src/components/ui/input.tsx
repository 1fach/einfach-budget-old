import { styled, type HTMLStyledProps } from '@one-ui/styled-system/jsx'
import { input } from '@one-ui/styled-system/recipes'

export const Input = styled('input', input)
export type InputProps = HTMLStyledProps<typeof Input>
